name = "SHA-512 for files integrity"
desc = "This script receives two files as input and verifies if those two files has the same content by generating a hash for both files"
epilog = "For more information. see README.md"
inputs = "files to be analized"
